DATABASE_URL=postgresql://user:password@localhost:5432/eggfarm
PORT=3000
# Origin allowed to call this API via CORS — defaults to the Vite dev server if unset.
CLIENT_ORIGIN=http://localhost:5173
# Signs login session tokens. Generate with:
#   node -e "console.log(require('crypto').randomBytes(48).toString('base64'))"
JWT_SECRET=
